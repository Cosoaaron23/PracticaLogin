<Window x:Class="PracticaLogin.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmación" Height="250" Width="450"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        ResizeMode="NoResize"
        Background="#121212"
        AllowsTransparency="True">
    <Border x:Name="MainBorder" BorderBrush="#00E5FF" BorderThickness="2" CornerRadius="10" Background="#1E1E1E">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.8"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="lblTitulo" Text="CONFIRMACIÓN" 
                       Foreground="White" FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <Border Grid.Row="0" VerticalAlignment="Bottom" Height="1" Background="#333" Margin="0,10,0,0"/>

            <TextBlock x:Name="lblMensaje" Grid.Row="1" 
                       Text="¿Estás seguro de que quieres realizar esta acción?" 
                       Foreground="#CCCCCC" FontSize="14" TextWrapping="Wrap" 
                       TextAlignment="Center" VerticalAlignment="Center" Margin="10"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">

                <Button x:Name="btnCancelar" Content="CANCELAR" Click="BtnCancelar_Click"
                        Width="120" Height="35" Margin="0,0,15,0"
                        Background="Transparent" Foreground="White" BorderBrush="#555" BorderThickness="1"
                        Cursor="Hand" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="btnConfirmar" Content="CONFIRMAR" Click="BtnConfirmar_Click"
                        Width="120" Height="35"
                        Background="#00E5FF" Foreground="#121212" BorderThickness="0"
                        Cursor="Hand" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

            </StackPanel>
        </Grid>
    </Border>
</Window>