<Window x:Class="PracticaLogin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AKAY Launcher" Height="500" Width="850"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None"
        Icon="/Assets/logo.ico">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#B71C1C">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <Image Source="/Assets/logo.png" 
                       Width="240" 
                       RenderOptions.BitmapScalingMode="HighQuality">
                    <Image.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.6"/>
                    </Image.Effect>
                </Image>

                <TextBlock Text="PLAYER ONE" FontSize="18" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,20,0,0" Opacity="0.9">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" ShadowDepth="2" BlurRadius="2"/>
                    </TextBlock.Effect>
                </TextBlock>

            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="#121212">
            <StackPanel VerticalAlignment="Center" Margin="60">

                <TextBlock Text="BIENVENIDO" FontSize="32" FontWeight="Bold" Foreground="#00E5FF" Margin="0,0,0,30" HorizontalAlignment="Left">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00E5FF" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
                    </TextBlock.Effect>
                </TextBlock>

                <Label Content="USUARIO" Foreground="#AAAAAA" FontSize="12" FontWeight="Bold"/>
                <TextBox x:Name="txtUsuario" Height="40" Background="#1E1E1E" Foreground="White" 
                         BorderBrush="#333333" BorderThickness="1" Padding="10" FontSize="14" Margin="0,0,0,20"
                         VerticalContentAlignment="Center"/>

                <Label Content="CONTRASEÑA" Foreground="#AAAAAA" FontSize="12" FontWeight="Bold"/>
                <PasswordBox x:Name="txtPassword" Height="40" Background="#1E1E1E" Foreground="White" 
                             BorderBrush="#333333" BorderThickness="1" Padding="10" FontSize="14" Margin="0,0,0,30"
                             VerticalContentAlignment="Center"/>

                <Button x:Name="btnLogin" Content="INICIAR SESIÓN" Click="BtnLogin_Click"
                        Height="45" Background="#00E5FF" Foreground="#121212" FontWeight="Bold" FontSize="16"
                        BorderThickness="0" Cursor="Hand" Margin="0,0,0,20">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" x:Name="btnRegistrar" Content="CREAR CUENTA" Click="BtnRegistrar_Click"
                            Height="35" Background="Transparent" Foreground="#00E5FF" BorderBrush="#00E5FF" BorderThickness="2"
                            FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="3"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Grid.Column="2" x:Name="btnSalir" Content="SALIR" Click="BtnSalir_Click"
                            Height="35" Background="#333333" Foreground="White" BorderThickness="0"
                            FontWeight="Bold" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="3"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <TextBlock x:Name="lblMensaje" Text="" Foreground="#B71C1C" Margin="0,25,0,0" HorizontalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>