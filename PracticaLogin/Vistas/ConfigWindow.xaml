<Window x:Class="PracticaLogin.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuración" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Background="#121212"
        Icon="/Assets/logo.ico"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="TabMenuButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="8">
                            <Grid>
                                <Border x:Name="indicator" Width="4" HorizontalAlignment="Left" Background="Transparent" CornerRadius="2" Height="20" Margin="0"/>
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="25,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#1AFFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#3300E5FF"/>
                                <Setter TargetName="indicator" Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#333"/>
                                <Setter Property="Foreground" Value="#00E5FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Template="{DynamicResource ComboBoxToggleButton}" 
                                          Grid.Column="2" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}" Template="{DynamicResource ComboBoxTextBox}" 
                                     HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,3,23,3"
                                     Focusable="True" Background="Transparent" Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1E1E1E" BorderThickness="1" BorderBrush="#333"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="5" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1">
                <Path x:Name="Arrow" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Fill="#00E5FF" Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Border>
        </ControlTemplate>
        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="0,5,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#F04747"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#181818" BorderBrush="#333" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="30,40,0,40">
                    <TextBlock Text="AJUSTES" FontSize="28" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="AKAY CONFIG" FontSize="12" Foreground="#00E5FF" Margin="2,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="15,0">
                    <RadioButton Content="GENERAL" GroupName="Menu" Style="{StaticResource TabMenuButton}" IsChecked="True" Click="BtnGeneral_Click"/>
                    <RadioButton Content="GRÁFICOS" GroupName="Menu" Style="{StaticResource TabMenuButton}" Click="BtnGraficos_Click"/>
                    <RadioButton Content="SONIDO" GroupName="Menu" Style="{StaticResource TabMenuButton}" Click="BtnSonido_Click"/>
                    <RadioButton Content="CUENTA" GroupName="Menu" Style="{StaticResource TabMenuButton}" Click="BtnCuenta_Click"/>
                </StackPanel>

                <Button Grid.Row="2" Content="🡠 Volver al Inicio" Click="BtnVolver_Click" 
                        Margin="20,0,20,30" Height="45"
                        Background="#222" Foreground="#AAA" BorderThickness="0" 
                        Cursor="Hand" FontSize="14">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bdr" Property="Background" Value="#333"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" MouseDown="Window_MouseDown">
                <Button Content="✕" Click="BtnCerrar_Click" Style="{StaticResource CloseBtn}" Width="45" HorizontalAlignment="Right"/>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,5,0">
                <Grid Margin="50,10,50,10">

                    <StackPanel x:Name="pnlGeneral" Visibility="Visible">
                        <TextBlock Text="Configuración General" FontSize="32" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Preferencias básicas del lanzador" Foreground="#666" Margin="0,0,0,40"/>

                        <TextBlock Text="Idioma del Sistema" Foreground="#CCC" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <ComboBox x:Name="cmbIdioma" Margin="0,0,0,25">
                            <ComboBoxItem Content="Español (España)" Tag="es"/>
                            <ComboBoxItem Content="English (US)" Tag="en"/>
                            <ComboBoxItem Content="Français" Tag="fr"/>
                        </ComboBox>

                        <TextBlock Text="Directorio de Instalación" Foreground="#CCC" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <Grid Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="txtDirectorio" Text="C:\Games\AkayLibrary" IsReadOnly="True" Foreground="#888"/>

                            <Button Grid.Column="1" Content="..." Click="BtnSeleccionarDirectorio_Click" 
                                    Width="40" Background="#333" Foreground="White" Margin="10,0,0,0" 
                                    BorderThickness="0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </Grid>

                        <Separator Background="#333" Margin="0,10,0,20"/>

                        <CheckBox x:Name="chkInicioWindows" Content="Iniciar AKAY al arrancar Windows" Foreground="White" Margin="0,0,0,15" FontSize="14"/>
                        <CheckBox x:Name="chkMinimizarBandeja" Content="Minimizar a la bandeja del sistema al cerrar" Foreground="White" Margin="0,0,0,15" FontSize="14"/>
                        <CheckBox x:Name="chkAutoUpdate" Content="Descargar actualizaciones automáticamente" Foreground="White" Margin="0,0,0,30" FontSize="14"/>
                    </StackPanel>

                    <StackPanel x:Name="pnlGraficos" Visibility="Collapsed">
                        <TextBlock Text="Gráficos y Rendimiento" FontSize="32" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Ajusta la calidad visual de la interfaz" Foreground="#666" Margin="0,0,0,40"/>

                        <TextBlock Text="Resolución de Ventana" Foreground="#CCC" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <ComboBox x:Name="cmbResolucion" Margin="0,0,0,25">
                            <ComboBoxItem Content="1280 x 720" Tag="1280,720"/>
                            <ComboBoxItem Content="1600 x 900" Tag="1600,900"/>
                            <ComboBoxItem Content="1920 x 1080" Tag="1920,1080"/>
                        </ComboBox>

                        <TextBlock Text="Calidad de Renderizado" Foreground="#CCC" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <Slider x:Name="sldCalidad" Minimum="0" Maximum="100" Value="80" Margin="0,0,0,25" TickFrequency="10" IsSnapToTickEnabled="True" Foreground="#00E5FF"/>

                        <CheckBox x:Name="chkVsync" Content="Activar V-Sync (Evitar tearing)" Foreground="White" Margin="0,0,0,15" FontSize="14"/>
                        <CheckBox x:Name="chkFps" Content="Mostrar contador de FPS" Foreground="White" Margin="0,0,0,15" FontSize="14"/>
                        <CheckBox x:Name="chkBajoRendimiento" Content="Modo Bajo Rendimiento (Ahorro de batería)" Foreground="White" Margin="0,0,0,15" FontSize="14"/>
                    </StackPanel>

                    <StackPanel x:Name="pnlSonido" Visibility="Collapsed">
                        <TextBlock Text="Audio" FontSize="32" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Controla el volumen y dispositivos" Foreground="#666" Margin="0,0,0,40"/>

                        <TextBlock Text="Volumen Maestro" Foreground="#CCC" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <Grid Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔊" Foreground="White" VerticalAlignment="Center"/>
                            <Slider x:Name="sldVolumen" Grid.Column="1" Minimum="0" Maximum="100" Value="100" Foreground="#00E5FF"/>
                            <TextBlock Text="{Binding ElementName=sldVolumen, Path=Value, StringFormat={}{0:0}%}" Grid.Column="2" Foreground="#888" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>

                        <TextBlock Text="Dispositivo de Salida" Foreground="#CCC" Margin="0,0,0,8" FontWeight="SemiBold"/>
                        <ComboBox x:Name="cmbAudioDevice" Margin="0,0,0,25">
                            <ComboBoxItem Content="Predeterminado del Sistema"/>
                            <ComboBoxItem Content="Auriculares (Realtek HD Audio)"/>
                            <ComboBoxItem Content="Altavoces (USB Audio)"/>
                        </ComboBox>

                        <CheckBox x:Name="chkSilenciarMinimizado" Content="Silenciar audio al minimizar" Foreground="White" Margin="0,0,0,15" FontSize="14"/>
                    </StackPanel>

                    <StackPanel x:Name="pnlCuenta" Visibility="Collapsed">
                        <TextBlock Text="Mi Cuenta" FontSize="32" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Gestiona tu seguridad y suscripción" Foreground="#666" Margin="0,0,0,40"/>

                        <Border Background="#1E1E1E" CornerRadius="12" Padding="25" Margin="0,0,0,30" BorderBrush="#333" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#333" CornerRadius="30" Width="60" Height="60">
                                    <TextBlock Text="👤" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                    <TextBlock x:Name="lblUsuarioCuenta" Text="USUARIO" Foreground="White" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock x:Name="lblIdCuenta" Text="ID: #0000" Foreground="#00E5FF" FontSize="14"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <TextBlock Text="Seguridad" Foreground="#00E5FF" Margin="0,0,0,15" FontWeight="Bold" FontSize="16"/>

                        <Button Content="Cambiar Contraseña" Click="BtnMostrarCambioPass_Click" 
                                HorizontalAlignment="Left" Width="200" Height="35"
                                Background="#222" Foreground="White" BorderBrush="#444" BorderThickness="1">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Border x:Name="pnlCambioPass" Background="#181818" BorderBrush="#00E5FF" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,15,0,0" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="Nueva contraseña" Foreground="#AAA" Margin="0,0,0,5"/>
                                <PasswordBox x:Name="txtNuevaPass" Margin="0,0,0,15"/>
                                <Button Content="Guardar Nueva Contraseña" Click="BtnConfirmarPass_Click" 
                                        Background="#00E5FF" Foreground="Black" FontWeight="Bold" Height="35"
                                        Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>

                        <Separator Background="#333" Margin="0,30"/>

                        <TextBlock Text="Zona de Peligro" Foreground="#F04747" Margin="0,0,0,15" FontWeight="Bold" FontSize="16"/>

                        <Button Content="Gestionar Suscripción" Click="BtnGestionarSuscripcion_Click" 
                                HorizontalAlignment="Left" Width="200" Height="35" Margin="0,0,0,10"
                                Background="Transparent" Foreground="White" BorderBrush="#555" BorderThickness="1">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button Content="Cerrar Sesión Global" 
                                HorizontalAlignment="Left" Width="200" Height="35"
                                Background="#1A1212" Foreground="#F04747" BorderBrush="#F04747" BorderThickness="1">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#121212" BorderBrush="#333" BorderThickness="0,1,0,0" Padding="20">
                <Button Content="GUARDAR CAMBIOS" Click="BtnGuardar_Click" 
                        Height="50" HorizontalAlignment="Stretch"
                        Background="#00E5FF" Foreground="#121212" FontWeight="Bold" FontSize="16" Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect Color="#00E5FF" Direction="270" ShadowDepth="4" Opacity="0.3" BlurRadius="15"/>
                    </Button.Effect>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bdr" Property="Background" Value="#9900E5FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                </Button>
            </Border>
        </Grid>
    </Grid>
</Window>