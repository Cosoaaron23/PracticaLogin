<Window x:Class="PracticaLogin.GameDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalles del Juego" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Background="#121212" BorderBrush="#333" BorderThickness="1">

    <Grid>
        <Grid>
            <Image x:Name="imgFondo" Stretch="UniformToFill" Opacity="0.2"/>
            <Rectangle Fill="#E6121212"/>
        </Grid>

        <Button Content="✕" Click="BtnCerrar_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20"
                Background="Transparent" Foreground="White" FontSize="20" BorderThickness="0" Cursor="Hand"/>

        <Grid Margin="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="10" BorderBrush="#333" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
                </Border.Effect>

                <Grid>
                    <Image x:Name="imgPortada" Stretch="UniformToFill"/>
                    <Border x:Name="tagOnline" Background="#FF4081" VerticalAlignment="Top" HorizontalAlignment="Right" 
                            CornerRadius="0,10,0,10" Padding="15,5" Visibility="Collapsed">
                        <TextBlock Text="ONLINE" Foreground="White" FontWeight="Bold" FontSize="12"/>
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Margin="40,0,0,0" VerticalAlignment="Center">

                <TextBlock x:Name="lblTitulo" Text="TÍTULO DEL JUEGO" FontSize="40" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
                <TextBlock x:Name="lblGenero" Text="ACCIÓN • RPG • AVENTURA" FontSize="16" Foreground="#00E5FF" Margin="0,5,0,20"/>

                <Separator Background="#333" Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="TAMAÑO" Foreground="#888" FontSize="12" FontWeight="Bold"/>
                        <TextBlock x:Name="lblEspacio" Text="45 GB" Foreground="White" FontSize="16"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="JUGADORES" Foreground="#888" FontSize="12" FontWeight="Bold"/>
                        <TextBlock x:Name="lblJugadores" Text="Un Jugador" Foreground="White" FontSize="16"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Sumérgete en esta increíble aventura llena de acción y gráficos impresionantes. Experimenta la nueva generación de juegos con AKAY Launcher." 
                           Foreground="#BBB" TextWrapping="Wrap" Margin="0,0,0,30" LineHeight="22"/>

                <StackPanel>

                    <Grid x:Name="pnlDescarga" Visibility="Collapsed" Margin="0,0,0,10">
                        <ProgressBar x:Name="progressBar" Height="10" Background="#333" Foreground="#00E5FF" BorderThickness="0" Value="0" Maximum="100"/>
                        <TextBlock x:Name="lblEstadoDescarga" Text="Descargando... 0%" Foreground="White" HorizontalAlignment="Center" Margin="0,15,0,0" FontSize="12"/>
                    </Grid>

                    <Button x:Name="btnAccion" Content="AÑADIR AL CARRITO" Click="BtnAccion_Click" 
                            Height="55" FontSize="18" FontWeight="Bold" Cursor="Hand"
                            Background="#00E5FF" Foreground="#121212" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <TextBlock x:Name="lblPrecio" Text="49,99 €" Foreground="#888" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>

            </StackPanel>
        </Grid>
    </Grid>
</Window>