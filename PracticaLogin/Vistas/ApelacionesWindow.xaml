<Window x:Class="PracticaLogin.ApelacionesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Buzón de Apelaciones" Height="500" Width="850"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Background="#151515" BorderBrush="#E65100" BorderThickness="1">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BUZÓN DE APELACIONES" Foreground="#E65100" FontSize="24" FontWeight="Bold" Margin="0,0,0,20">
            <TextBlock.Effect>
                <DropShadowEffect Color="#E65100" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
            </TextBlock.Effect>
        </TextBlock>

        <DataGrid x:Name="dgMensajes" Grid.Row="1" 
                  Background="#1E1E1E" RowBackground="#252525" Foreground="White" 
                  BorderBrush="#333" HeadersVisibility="Column" 
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#444">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333"/>
                    <Setter Property="Foreground" Value="#E65100"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="BorderBrush" Value="#151515"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                </Style>

                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="FECHA" Binding="{Binding FechaLegible}" Width="140"/>

                <DataGridTextColumn Header="USUARIO" Binding="{Binding Username}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#00E5FF"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="MENSAJE" Binding="{Binding Mensaje}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="ACCIÓN" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="BORRAR" Click="BtnEliminar_Click" 
                                    Background="#CC0000" Foreground="White" FontWeight="Bold" 
                                    FontSize="10" Padding="5,2" BorderThickness="0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="3"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="CERRAR" Click="BtnCerrar_Click" Width="120" Height="40" 
                Background="#333" Foreground="White" BorderThickness="0" FontWeight="Bold"
                HorizontalAlignment="Right" Margin="0,20,0,0" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>